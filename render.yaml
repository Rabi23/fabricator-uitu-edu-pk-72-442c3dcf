services:
  - type: web
    name: uitu-edu-pk-public
    runtime: python
    plan: free
    region: oregon
    buildCommand: pip install -r requirements.txt && python bootstrap_render.py
    startCommand: gunicorn render_entry:app --bind 0.0.0.0:$PORT
    healthCheckPath: /health
